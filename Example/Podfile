source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'LazyDays_Example', :exclusive => true do
  pod 'LazyDays', :path => '../'
end

target 'LazyDays_Tests', :exclusive => true do
  pod 'LazyDays', :path => '../'

  pod 'Quick', '~> 0.8.0'
  pod 'Nimble', '3.0.0'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
end
